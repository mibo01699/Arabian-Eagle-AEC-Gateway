{
  "name": "arabian-eagle-aec-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "Arabian Eagle AEC Gateway",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest --runInBand",
    "test:security": "jest --runInBand tests/security.test.js",
    "test:integration": "jest --runInBand tests/integration.test.js",
    "test:all": "jest --runInBand",
    "test:external": "jest --runInBand tests/external --passWithNoTests"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.0.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
